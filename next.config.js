/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  outputFileTracingRoot: '/Users/ranium/Desktop/health/holistic-health-buddy',
  images: {
    domains: ['localhost'],
  },
}

module.exports = nextConfig
