# ==============================================================================
# ENVIRONMENT VARIABLES - Copy this to .env.local and fill in the values
# ==============================================================================
# SECURITY: Never commit .env.local to git - add to .gitignore
# For production (Vercel), set these in: Settings > Environment Variables
#
# Full setup guide: See PRODUCTION_AUTH_SETUP.md
# ==============================================================================

# ========== SUPABASE CONFIGURATION ==========
# Get these values from: Supabase Dashboard > Project Settings > API

# Public Supabase URL (safe to expose)
# Example: https://abc123def.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Public Anon Key (safe to expose, used by client-side code)
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service Role Key (SECRET - server-side only)
# IMPORTANT: 
#   - Do NOT prefix with NEXT_PUBLIC_
#   - Keep this private - only available to server process
#   - Never share or commit to git
#   - For Vercel: Only set in Production environment for security
SUPABASE_SERVICE_ROLE_KEY=

# ========== OPTIONAL API KEYS ==========

# OpenRouter API Key (for AI features, optional)
# Get from: https://openrouter.ai/
OPENROUTER_API_KEY=

# ========== PRODUCTION NOTES ==========
# 1. Email Authentication:
#    - Supabase > Authentication > Providers > Email (enable)
#    - Supabase > Authentication > URL Configuration (set redirect URLs)
#    - Include: https://fitnest-fullstack.vercel.app/auth/callback
#
# 2. Vercel Deployment:
#    - Project > Settings > Environment Variables
#    - Add NEXT_PUBLIC_* vars for all environments
#    - Add SUPABASE_SERVICE_ROLE_KEY only for Production (security best practice)
#    - Redeploy after changes
#
# 3. Testing:
#    - Local: npm run dev (uses .env.local)
#    - Production: https://fitnest-fullstack.vercel.app/
#
# For full setup instructions, see: PRODUCTION_AUTH_SETUP.md